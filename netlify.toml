# Netlify configuration for the Velox House Vite + React Router site
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every route serves index.html so /tools works on direct load
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
